#include <chrono>
#include <iostream>
#include <graphics.h>
#include <thread>

int main() {
	std::cout << "Hello World!\n";

	Graphics graphics;

	while (true) {
		
	}
	// std::this_thread::sleep_for(std::chrono::seconds(10));
	return 0;
}
